[project]
name = "myproject"
version = "0.1.0"
description = "Интернет-магазин одежды на Django"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2,<5.3",
    "psycopg[binary]>=3.2",
    "pillow>=11.0",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = [
    "pylint",
]
